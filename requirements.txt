# OmniCount Dependencies
# Install with: pip install -r requirements.txt

# Core
torch>=1.10.0
torchvision>=0.11.0
numpy>=1.21.0
Pillow>=9.0.0
opencv-python>=4.5.0
scikit-image>=0.19.0
scipy>=1.7.0
matplotlib>=3.5.0

# Transformers & CLIP
transformers>=4.22.0
ftfy>=6.1.0
regex>=2022.0.0

# Baselines
timm>=0.6.0

# Utilities
tqdm>=4.62.0
json5>=0.9.0
pyyaml>=6.0

# Note: External submodules have their own dependencies.
# After cloning submodules, install them separately:
#   cd external/sam && pip install -e .
#   cd external/GroundingDINO && pip install -e .
#   cd external/SAN && pip install -r requirements.txt
#   cd external/Marigold && pip install -r requirements.txt
