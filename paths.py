from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent
OUTPUTS_ROOT = REPO_ROOT / "outputs_sota"
CLIPSEG_OUTPUTS_ROOT = REPO_ROOT / "outputs_clipseg"
HMAP_ROOT = REPO_ROOT / "hmap"
CHECKPOINTS_ROOT = REPO_ROOT / "checkpoints"
